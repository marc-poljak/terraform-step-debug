{
  "application": {
    "name": "${app_name}",
    "port": ${port},
    "priority": ${priority}
  },
  "storage": {
    "bucket": "${bucket_name}"
  },
  "security": {
    "database_password": "${db_password}",
    "api_key": "${api_key}",
    "api_token": "${api_token}"
  }
}